[package]
name = "civic-routing"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"
description = "Deterministic shortest paths for the OpenGlassBox resident simulation"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
codegen-units = 1
lto = "thin"
